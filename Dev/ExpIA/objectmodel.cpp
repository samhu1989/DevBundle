#include "objectmodel.h"
void ObjModel::update(MeshBundle<DefaultMesh>::Ptr input)
{
    //evaluate local density of input
    arma::fvec local_densiy_;
    //add new point if not update point if close enough
}
